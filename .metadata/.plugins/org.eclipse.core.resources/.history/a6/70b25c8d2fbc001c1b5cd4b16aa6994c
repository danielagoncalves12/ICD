package tp1.battleship;

import java.util.ArrayList;
import java.util.Collections;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

public class GameView {
	
	private Map<Integer, String> view = new HashMap<Integer, String>();

	public void printBorder() {
		
		System.out.print("   X ");
		for (int i = 0; i <= 28; i++) {
			System.out.print((i != 28) ? "-" : " X\n");
		}
	}
	
	/** 
	 * Método printBoard
	 * Apresenta o tabuleiro do jogo.
	 */
	public void printBoard(List<List<Integer>> jogo) {
		
		view.put(0, new String(" ")); // Por explorar (Sem navio)
	    view.put(1, new String(" ")); // Por explorar (Com navio)
	    view.put(2, new String("X")); // Pressionado (Sucesso)
	    view.put(3, new String(".")); // Pressionado (Sem sucesso)
		
		String[] letters = {"A", "B", "C", "D", "E", "F", "G", "H", "I", "J"};
		
		// Apresentação das letras
		System.out.print("    ");
		for (int i = 0; i < 10; i++) {
			System.out.print(" " + letters[i] + " ");
		}
		System.out.println();

		// Apresentação da borda do tabuleiro
		printBorder();
		
		// Apresentação do conteúdo do tabuleiro
		for (int i = 0; i < 10; i++) {
			
			System.out.print(i+1 + " ");
			System.out.print((i != 9) ? " |" : "|");
			for (int j = 0; j < 10; j++) {
			
				int value = jogo.get(i).get(i);
				System.out.print(" " + value + " ");
				System.out.print((j == 9) ? "|" : "");
			}
			System.out.println();
		}
		
		// Apresentação da borda do tabuleiro
		printBorder();
	}
	
	public static void main(String[] args) {
		
		List<List<Integer>> jogo = new ArrayList(Collections.nCopies(10, new ArrayList(Collections.nCopies(10, 0))));

		new GameView().printBoard(jogo);
	}
}
